name: CI and Docker Build

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Log in to Docker Hub
      uses: docker/login-action@v3
      with:
        username: 02210199
        password: ${{ secrets._02210199 }}

    - name: Build Docker image
      run: docker build -t 02210199/jwt-playground:latest .

    - name: Push Docker image
      run: docker push 02210199/jwt-playground:latest
